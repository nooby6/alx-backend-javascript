/* eslint-disable no-unused-vars */
export default function getResponseFromAPI() {
  return new Promise((resolve, reject) => {
    // Resolve the promise with some value (or without any value)
    resolve();
  });
}
